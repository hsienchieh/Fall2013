 # start: b[i]:=1 
        # x:=i 
        # if y!=0: b[i]:=0 
                 # await y=0 
                 # goto start fi 
        # y:=i
        # if x!=i: b[i]:=0
                 # for j:=1..n: await b[j]=0 
                 # if y!=i: await y=0
                          # goto start fi fi 
        # CS 
        # y:=0 
        # b[i]:=0 
		
def lambortFast()
    b[i]=1
	if y!=0:
	    b[i]=0
		while y=0:
		    pass
		
